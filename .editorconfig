# Added so that github shows tabs with an indent size
# of 4 spaces
#
# Matches multiple files with brace expansion notation
[*.{c,cpp,h}]
end_of_line = crlf
insert_final_newline = true

indent_style = space
indent_size = 4